# Test Report – Medical Appointment System

## Backend smoke test
- Python compileall: PASS
- 4 smoke scenarios: PASS
- Authentication / JWT flow: PASS trong test environment
- Booking: tạo lịch, chống trùng slot, đổi lịch, hủy lịch và tái sử dụng slot: PASS
- Reception: tra cứu bệnh nhân, check-in, waiting queue, gọi bệnh nhân, WALK_IN, NO_SHOW: PASS
- Doctor: phân quyền, WAITING → IN_PROGRESS → COMPLETED và medical result: PASS

## Deployment fixes included
- Email demo dạng `@medschedule.local` không còn bị chặn bởi EmailStr.
- Error 422 được trả dưới dạng thông báo ổn định cho frontend.
- SPA routing cho Vercel được cấu hình bằng `frontend/vercel.json`.
- Slot đã hủy / NO_SHOW được giải phóng bằng partial unique index để có thể đặt lại.
- Lễ tân tra cứu theo mã lịch / mã bệnh nhân / họ tên / SĐT / email.
- UI lễ tân và bác sĩ dùng tiếng Việt; status nội bộ vẫn giữ nguyên theo đặc tả.

## Frontend build note
Môi trường kiểm thử hiện tại không có quyền truy cập npm registry và không có sẵn toàn bộ frontend dependency cache, nên không thể chạy `npm install` / `vite build` trong container này. Source frontend đã được kiểm tra cấu trúc/import thủ công; production build thực tế đã được Vercel xác nhận ở các deployment trước.

## Payment extension
- Price catalog by specialty: Tim mạch 350.000đ; Nội tổng quát 250.000đ; Nhi khoa 300.000đ (sample/demo prices, not hospital price lists).
- QR flow: PENDING -> PAID.
- Cancellation after PAID: REFUND_PENDING.
- Offline flow: UNPAID and pay-at-clinic.
- QR payload/image generated by backend for demo.
